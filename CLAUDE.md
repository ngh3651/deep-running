# Deep Running — 작업 규칙

이 저장소의 스펙은 SPEC.md다. 스펙과 코드가 다르면 **둘 중 하나가 낡은 것**이니 어느 쪽인지 판단해서 고쳐라.
초기 설계는 정답이 아니다. 더 나은 안이 보이면 SPEC.md를 고치고 README 결정 로그에 왜 바꿨는지 남겨라.
사람(규혁)은 거의 개입하지 않는다. 질문으로 멈추지 말고, 아래 규칙으로 스스로 결정하라.

## 원칙 4개
1. Think Before Coding — 짐작 금지. 스펙에 없는 결정은 '기본값 규칙'으로 정하고,
   그래도 애매하면 가장 단순한 안을 골라 README 맨 아래 '결정 로그'에 한 줄 남긴다.
2. Simplicity First — 문제를 푸는 최소한의 코드만. 추상화 레이어, 상태관리 라이브러리,
   서버 코드 금지. 200줄 썼는데 50줄이면 되겠다 싶으면 다시 쓴다.
3. Surgical Changes — 시키지 않은 리팩토링·개선·포맷 정리 금지.
   이번 작업 범위 밖의 파일은 건드리지 않는다.
4. Goal-Driven Execution — 완료 판정을 전부 통과하기 전에는 완료라고 보고하지 않는다.

## 기본값 규칙 (스펙에 없을 때)
- UI 문구는 한국어 해요체로 짧게. 영어 문구 금지 (고유명사·단위 예외)
- 값이 바뀌는 자리 뒤에 조사를 하드코딩하지 않는다 — `src/lib/ko.ts` 의 `ro()` `iga()` 를 쓴다
- 색·간격·폰트는 SPEC.md 디자인 토큰만 사용. 새 색이 필요하면 `color-mix` 로 토큰에서 파생시킨다
- 색의 역할은 하나씩이다: 주황 = 나·지금·주요 동작 / 초록 = **내 과거보다** 나아진 것 /
  빨강 = 지우기·실패 / 금색 = 성취·1위. **남보다 못한 걸 빨강으로 칠하지 않는다**
- 저장할지 계산할지 애매하면 계산한다 (페이스·스트릭·누적·랭킹·뱃지·등급은 전부 계산값)
- 의존성 추가는 SPEC.md 허용 목록 안에서만

## 검증 — 한 줄이면 된다

```
npm run verify      # 타입 검사 · 유닛 테스트 · 규율 검사 · 빌드
npm run shots       # 전 화면 스크린샷 → .shots/contact.png 로 한 장에 모아본다
```

- `npm run verify` 가 초록이 아니면 완료가 아니다. CI도 같은 걸 돌린다
- `npm run shots` 로 찍고 **`.shots/contact.png` 를 직접 눈으로 봐라.**
  깨진 레이아웃, 안 읽히는 대비, 빈 화면, 영어 문구가 보이면 완료가 아니다
- `2x-thin-*` 은 멤버 1명·기록 1건짜리 화면이다. 지금 배포된 DB가 그 상태라 **여기가 제일 중요하다**
- 스크린샷은 가짜 데이터를 REST에 물려 찍는다 — 운영 DB를 건드리지 않는다 (`scripts/rest-mock.mjs`)
- 완료 판정 항목별 결과를 한 줄씩 보고한다

## 서브 에이전트 (`.claude/agents/`)

혼자 오래 보면 눈이 먼다. 한 덩어리 끝낼 때마다 남의 눈을 빌려라.

- `design-reviewer` — 스크린샷을 보고 디자인 문제를 잡는다. `npm run shots` 뒤에 돌린다
- `copy-auditor` — 한국어 문구의 조사·띄어쓰기·말투를 본다. 문구를 손댄 뒤에 돌린다
- `spec-auditor` — 코드와 SPEC.md가 어긋난 곳을 찾는다. 기능을 끝낸 뒤에 돌린다

## 브랜치
- 기본은 main에서 바로 작업하고 자주 커밋한다. 커밋 하나가 한 가지 일이 되게 잘게 쪼갠다
- 큰 기능(여러 화면을 갈아엎는 수준)일 때만 feat/* 브랜치를 판다. 그때는 PR 본문에
  무엇을·왜·어떻게 검증했는지 한국어로 짧게 쓴다
- CI(tsc·vitest·guard·build)는 main push와 PR 양쪽에서 돈다
- 빌드·테스트가 빨간 채로 푸시하지 않는다

## 파일 규율
- SPEC.md 10장의 폴더 구조 밖에 파일을 만들지 않는다. `scripts/guard.mjs` 가 이걸 검사한다
- 구조를 넓혀야 하면 SPEC.md 10장을 먼저 고친다
- 문서는 SPEC.md, CLAUDE.md, README.md 셋뿐. 작업 요약·보고용 .md 생성 금지 (보고는 대화로)
- 커밋은 잘게, 메시지는 한국어 한 줄. force push 금지

## DB를 바꿔야 할 때
Claude에게 이 Supabase 프로젝트 권한이 없다. 스키마 변경은 `supabase/migrations/` 에 SQL을 쓰고,
**규혁이 콘솔에서 직접 돌릴 때까지 앱이 그 기능 없이도 멀쩡히 돌아가게** 만든다
(`src/lib/store.ts` 의 `probeCaps` 참고 — 없으면 그 기능을 조용히 감춘다).
그리고 붙여넣을 SQL을 대화로 그대로 알려준다.

## 보고
- 결론부터, 짧은 산문으로. 목록 남발 금지
- 못 한 것과 불확실한 것은 그렇다고 말한다. 추측을 사실처럼 쓰지 않는다
