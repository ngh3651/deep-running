# Deep Running — 기획서 v2 (실행 스펙)

> 2026-08-26 · 소모임장 규혁 · **이 문서 하나로 Claude Code가 처음부터 배포까지 완성하는 것이 목표**
>
> v1(제품 기획)에 실행 스펙 — 화면 상세, 계산 규칙, DB, 디자인 토큰, 작업 규칙(CLAUDE.md), Phase별 완료 판정 — 을 붙였다. 스펙과 코드가 다르면 **스펙이 맞다.**

## 0. 사용법 (규혁용 — 이것만 읽어도 됨)

1. 새 폴더에 이 파일을 `SPEC.md`로 저장한다
2. Claude Code를 열고 이렇게 말한다:

```
SPEC.md 읽어. 11장의 CLAUDE.md를 먼저 만들고, 12장 Phase 0부터 순서대로 진행해.
```

3. 사람이 할 일은 13장에 적힌 것이 전부다. 나머지는 전부 Claude Code가 스스로 결정하고 검증한다.

---

# PART A. 제품

## 1. 한 줄 정의

인하대 인공지능공학과 러닝 소모임 **Deep Running**(7명, 소모임장 규혁)의 인증 기록을 한곳에 모아, 혼자 쓰는 러닝앱이 못 주는 **"같이 달리는 재미"**를 만드는 소모임 전용 모바일 웹.

지금은 각자 나이키런클럽·삼성헬스로 달리고 톡방에 스크린샷으로 "오늘도 달렸습니다" 인증을 올린다. 이 인증이 톡방에 흘러가 버려 쌓이지 않는 게 아쉬움이고, 코딩하는 사람들이 이름까지 Deep Running인데 그냥 달리기만 하면 재미없다는 게 출발점이다.

## 2. 원칙 (판단 기준)

1. **기존 러닝앱과 경쟁하지 않는다.** 개인 기록 측정은 나이키런클럽이 잘한다. 우리는 소모임에만 있는 재미를 만든다.
2. **톡방을 대체하지 않고 보완한다.** 웹은 기록이 쌓이고 굴러가는 곳.
3. **경쟁보다 꾸준함.** 랭킹 1순위는 거리가 아니라 인증 횟수·스트릭. (기록 경쟁은 부상·초보 소외를 부른다)
4. **짜쳐도 우리만의 기능을, 예쁘게.**

## 3. 기능 범위

**있는 것 전부:** 로그인(이름 + 숫자 4자리, 첫 입력이 곧 가입) · 인증 업로드(사진에서 거리·시간 자동 인식, 퀵칩, 올린 직후 보상 화면) · 기록 고치기·지우기 · 피드(날짜별, 기록마다 한마디) · 마이(등급·잔디·성장 지표·최고 기록·에딩턴 수·뱃지·소모임 비교) · 홈(가상 종주 지도, 이번 주 요약, 소모임원 목록) · 랭킹(꾸준함·거리·페이스, 케이던스는 DB가 준비되면) · 톡방 공유 카드 · 홈 화면에 추가(PWA) · 건의함

**아직 안 만든 것:** 스크린샷 보관, 푸시 알림, GPS 아트, 별도 회원가입·비번찾기 화면, 관리자 화면, 다크/라이트 전환(다크 고정), 지도 타일·런타임 지도 API·지도 라이브러리, 무한 스크롤, 서버 코드

**의도적으로 안 하기로 한 것 (근거 있음):**
- **절대 거리·속도 랭킹을 1순위로 두지 않는다.** 상위권만 동기부여되고 나머지가 이탈한다. 7명이면 하위 3명이 매주 바닥에 있는 자기 이름을 본다 (원칙 3)
- **영구 누적 랭킹만 두지 않는다.** 늦게 들어온 사람이 수학적으로 못 따라잡는다 — 그래서 기본이 '이번 주'다
- **연대책임 스트릭**(한 명이 빠지면 전원 0) — 매주 누군가는 시험이거나 감기다. 사회적 처벌은 조용한 탈퇴로 끝난다
- **VO2max·ACWR 부상 위험 같은 파생 지표** — 전력 레이스를 전제한 공식이라 동네 조깅 데이터를 넣으면 전원이 저평가된다. Riegel 환산까지만 쓴다
- **백분위 표시** — 7명에선 순위와 수학적으로 같은 말이라 새로운 정보가 없다. 값을 나란히 놓는 비교 막대로 대신한다

**2차 백로그:** 콕 찌르기 · 주간 목표 개인화 · LLM 주간 리캡 · 세계 일주 루트 · 시즌 리셋

# PART B. 실행 스펙 (Claude Code용)

## 4. 화면 상세 (모바일 웹, 다크 고정, 하단 탭 5개)

하단 탭: **홈 · 피드 · ➕업로드(중앙, 강조 원형) · 랭킹 · 마이**. 로그인 전엔 로그인 화면만 접근 가능.

### 4.1 로그인 `/login`

**회원가입 화면은 없다.** 이름 + 숫자 4자리, 두 칸이 전부다. 처음 친 조합이 곧 내 계정이고, 다음부터 같은 조합을 치면 내 기록이 그대로 이어진다.

- 입력: 이름(텍스트, 앞뒤 공백 자동 제거) / 비밀번호(숫자 4자리, `inputmode="numeric"`, 마스킹) → [시작하기]
- 이름이 DB에 **없으면**: 확인 다이얼로그 "'규혁'으로 새로 시작할까요?" → 예 → members insert 후 로그인. 오타로 유령 계정이 생기는 걸 막는 유일한 장치라 생략 금지
- 이름이 **있고 비번 일치** → 로그인. **있고 불일치** → "비밀번호가 달라요" + 흔들림. 이 경우 절대 새 계정을 만들지 않는다
- 이모지는 가입 시 자동 배정(입력 단계 추가 금지). 후보: 🏃 🐢 🔥 ⚡ 🌙 🍀 🦊 🐻 🐰 🦁
- 성공: `localStorage.dr_member = {id, name, emoji}` → 홈. **만료 없음** — 직접 로그아웃하기 전까지 유지 (매번 다시 치게 하면 안 쓴다)
- 비번 분실: 소모임장에게 말하면 규혁이 Supabase에서 해당 행을 지워 다시 만들게 안내

### 4.2 홈 `/`

- 머리: 오늘 날짜 + `{이름}님, 오늘도 달려요`
- **히어로 카드**: 소모임 **누적 거리** 큰 숫자 (카운트업 애니메이션 1개) + 아랫줄에 `이번 주 나 n회 · nn.nnkm`.
  누적만 크게 띄우면 내 상태가 화면 한참 아래로 밀린다
- **가상 종주 카드**: 한국 지도 위에 도시 10곳을 점으로 찍고, 지나온 구간은 `--accent`로, 남은 구간은 흐리게 그린다.
  현재 구간은 진행률만큼 `stroke-dasharray`로 펜이 그려지듯 채워진다(애니메이션 1개). 현재 위치 점은 천천히 숨쉰다.
  **지도에 도시 이름을 얹지 않는다** — 390px에서 라벨이 겹친다. 지도 아래 한 줄에 `출발지 ─막대─ 다음 도시`,
  그 아래 남은 km와 다음 보상. 카드 머리의 숫자는 `구간 n / 9` — 지도의 점과 맞아떨어지는 숫자여야 한다
  (전체 진행률 %는 초반에 늘 0%라 읽을 게 없다)
- 지도 높이는 300px로 묶는다. 한국은 세로로 긴 나라라 그대로 두면 화면 절반을 먹는다
- 지도 타일·런타임 지도 API·지도 라이브러리는 여전히 금지다. 저장소에 든 정적 SVG(`src/assets/korea.svg`)와
  미리 뽑아 `constants.ts`에 박은 좌표 배열만 쓴다 — 런타임 네트워크 호출 0건, 의존성 추가 0개
- **이번 주 소모임 카드**: 참여 원형 진행률(m/7) + 인증 n건 · 합계 km · **같이 달린 날**(2명 이상이 같은 날 달린 날) +
  최근 8주 막대 그래프
- **소모임원 목록**: 전원을 톡방 멤버 목록처럼 한 줄씩. 이모지 · 이름 · `이번 주 n회` 또는 `이번 주 아직이에요` ·
  🔥스트릭 · 주간 거리 막대. **등수를 붙이지 않는다** — 매주 꼴찌 이름을 보여주면 그 사람이 조용히 나간다.
  이번 주에 달린 사람이 위로 오고, 안 달린 사람은 흐리게만 한다 (X 표시·빨간색 금지)
- **홈 화면에 추가 안내**: 앱처럼 열려 있지 않고 아직 안 닫았을 때만 한 번. 닫으면 다시 안 뜬다
- 빈 상태: `첫 기록을 올리면 인하대에서 종주가 시작돼요` + **`첫 인증 올리기` 버튼**.
  누적 `0.00`을 화면에서 가장 큰 글자로 띄우지 않는다 — 첫 화면으로 최악이다

### 4.3 피드 `/feed`

- 최신순 50건 (페이지네이션·무한스크롤 없음, 새로고침 버튼 1개)
- **날짜로 묶는다** — `오늘` / `어제` / `8/25 화`
- RunCard — 사진 없이 숫자와 여백으로 승부하되 **밀도를 지킨다**. 한 건이 화면 3분의 1을 먹으면 50건이 그냥 흘러간다
  - 왼쪽 이모지 원 · 가운데 이름/페이스·시간/한마디/메모 · 오른쪽 거리 큰 숫자(30px)
  - **한마디**: 그 사람의 평소와만 견준다. `개인 최고 페이스`(금색) · `최장 거리`(금색) ·
    `평소보다 n초 빨랐어요`(초록) · `평소보다 멀리 달렸어요`(초록).
    **느렸다는 말은 하지 않는다** — 나쁜 소식을 알리는 게 목적이 아니라 계속 올리게 하는 게 목적이다
  - 기록이 3건 미만인 사람에겐 한마디를 붙이지 않는다 (견줄 평소가 없다)
- **응원**: 남의 기록에 하트 하나. 누른 즉시 화면이 바뀌고 서버에는 뒤따라 보낸다. 실패하면 되돌린다.
  내 기록에는 버튼이 없다 (숫자만 보인다). `cheers` 테이블이 없으면 아예 안 보인다

### 4.4 업로드 `/upload`

- 폼 순서: 사진(**선택**, 고르면 즉시 미리보기) → 거리 km → 시간 → 케이던스(선택, DB에 칸이 있을 때만) → 날짜 → 메모(선택, 60자) → [인증 올리기]
- 사진을 고르면 `숫자 읽는 중이에요` 스피너 → 클라이언트 OCR로 **거리·시간 칸을 채운다**. 자동 제출하지 않는다
- **읽은 결과를 확인 배너로 보여준다**: `사진에서 3.01km · 16:49 을 읽었어요. 다르면 아래에서 고쳐요`.
  한쪽만 읽혔으면 문장을 바꾼다 (`시간은 아래에 적어줘요`). 입력이 아니라 **확인**이 되게 하는 게 핵심이다
- **마찰 줄이기 장치**: 거리 퀵칩 `3km` `5km` `10km` `지난번 n.nnkm` · 시간이 비어 있으면
  `평소 페이스면 27:30` 칩(내 기록 3건 이상일 때 중앙값 페이스로 계산) · 날짜 칩 `오늘` `어제`
- 미리보기 아래 한 줄: `사진은 저장되지 않아요. 숫자만 읽고 바로 지워요`
- **사진은 서버에 올라가지 않는다.** 브라우저에서 읽고 버린다 (Storage 없음)
- OCR이 실패하면 조용히 넘어간다. 에러 문구 대신 `숫자를 못 찾았어요. 아래에 직접 적어줘요`
- **저장 성공 → 보상 화면.** 피드로 튕기지 않는다. 이 순간이 다음 인증을 부른다
  - `+n.nnkm` 큰 숫자 · 종주가 얼마나 남았는지 · 스트릭이 몇 주가 됐는지 · 새로 딴 뱃지가 있으면 그것
  - **내 기록이 소모임을 새 도시에 닿게 했으면** 도시 이모지와 `{도시} 도착!`을 크게 (`Boom`)
  - [톡방에 공유하기] — canvas로 그린 1080×1080 인증 카드를 공유 시트로 넘긴다 (없으면 파일로 떨군다)
- 검증 실패 문구는 구체적으로: `거리는 0.1~60km 사이로 적어줘요` / `시간 형식을 확인해줘요 (예: 16:49)` /
  `케이던스는 100~250 사이 숫자로 적어줘요`

**OCR 규칙 (`src/lib/ocr.ts` — 텍스트 파싱은 순수 함수로 분리해 유닛 테스트)**

```
엔진: tesseract.js, 영어 traineddata만 (jsdelivr CDN, 첫 인식 때 한 번 받고 캐시)
char whitelist: 0123456789:.'"  (숫자·구분자만 인식해 정확도를 올린다)
OCR 전 이미지는 긴 변 1280px로 축소한다 (browser-image-compression, 속도용)

텍스트에서 뽑는 순서
1) 거리 후보: /\d{1,2}[.,]\d{1,2}/ 전부 → 0.1~60 범위만 남기고 가장 큰 값
2) 시간 후보: /\d{1,2}:\d{2}(:\d{2})?/ 전부 → 초로 환산, 60~21600 밖은 버린다
   (단어 경계는 쓰지 않는다 — OCR이 통계 줄을 30:125'35"412 처럼 붙여 읽는다)
3) 시간 확정: 거리를 찾았으면 후보 중 '페이스가 2'30" ~ 15'00" 에 들어오는 것' 중
   가장 큰 값. 거리를 못 찾았으면 후보 중 가장 큰 값
4) 하나라도 못 찾으면 그 칸만 비워둔다
```

3번이 핵심이다. 나이키런클럽 화면엔 시간(`30:12`)과 페이스(`5:35`)가 같이 떠서 `HH:MM` 패턴만으로는 둘을 못 가른다. 이미 뽑은 거리로 나눠 페이스가 상식적인 범위에 드는 쪽을 고르면 갈린다.

**케이던스는 OCR로 읽지 않는다.** 숫자만 읽는 whitelist로는 `172 spm`과 `172 kcal`과 심박수를 가를 수 없고,
잘못 채우면 사용자가 알아채지 못한 채 틀린 값이 저장된다. 직접 입력만 받는다.

### 4.5 랭킹 `/ranking`

- 기간 세그먼트: **이번 주 / 이번 달 / 전체** (기본 이번 주)
- 지표 칩: **꾸준함 / 거리 / 페이스** (+ DB에 칸이 있으면 **케이던스**). 기본은 꾸준함이고, 나머지는 같은 사람들을 다른 눈으로 보는 재미다
- 3명 이상이면 **시상대**(1위가 가장 높다) + 4위부터 줄. 3명 미만이면 시상대 없이 줄만 — 한 명짜리 시상대는 이상하다
- 행: 순위 · 이모지 · 이름 · 값 막대 · 큰 값/작은 값. 내 행은 **테두리로만** 표시한다.
  같은 값인데 막대 색이 다르면 색이 '더 잘함'으로 읽힌다
- 정렬: 꾸준함 = 인증 횟수 ↓ → 거리 ↓ → 이름 가나다. 거리 = 거리 ↓ → (같으면 꾸준함 규칙).
  페이스·케이던스는 **그 기간에 3회 이상 달린 사람만** — 한 번 잘 뛴 사람이 요행으로 1등 하지 않게

### 4.6 마이 `/my`

- 프로필 카드: 이모지 · 이름 · **등급**(누적 거리로) · 다음 등급까지 남은 km + 원형 진행률
- 스탯 3개: 이번 달 km / 누적 km / 총 인증
- **스트릭 카드**: `🔥 n주 연속` + 꾸준함 점수 칩. 이번 주가 비었으면 `이번 주에 한 번만 올리면 불꽃이 이어져요`
  (안 한 걸 지적하지 않는다). 기록 3건 이상이면 **최근 15주 잔디**를 같이 보여준다
- **이번 달 내 변화**: 거리 % · 인증 회 · 평균 페이스. **지난달 같은 날짜까지와 견준다** —
  27일에 이번 달 27일치를 지난달 31일치와 비교하면 매달 초에 전원이 퇴보한 것처럼 보인다.
  지난달 기록이 없으면 카드를 아예 감춘다
- **주마다 얼마나 달렸어요**: 최근 10주 막대 + 페이스 흐름 선(빠를수록 위로 그린다, 기록 3건부터)
- **최고 기록**: 최장 거리 · 최고 페이스 · 5km 예상 기록(Riegel) + **에딩턴 수**와 다음 숫자까지 몇 번 남았는지.
  최고 페이스를 세운 지 21일이 넘으면 한 줄로 알려준다
- **뱃지**: 딴 것은 또렷하게, 못 딴 것은 흐리게 **전부** 보여준다 (감추면 뭘 노려야 할지 모른다) + 가장 가까운 목표 2개의 진행률
- **나와 소모임**: 주 평균 거리 / 평균 페이스를 값 그대로 나란히. 백분위·등수는 쓰지 않는다.
  느린 쪽을 빨강으로 칠하지 않는다
- **내 기록**: 6건까지 보이고 나머지는 `n건 더 보기`. 각 행 ⋯ 메뉴 → **고치기 / 공유하기 / 지우기**
  - 고치기: 사진에서 잘못 읽힌 숫자를 나중에 바로잡는 자리. `runs`에 update 정책이 없어서
    **새로 넣고 → 옛 걸 지운다** 순서로 한다. 반대로 하면 네트워크가 끊겼을 때 기록이 증발한다
  - 지우기: confirm `이 기록을 지울까요?`, 하드 삭제
- **건의함**: 누구나 소모임장에게 보낸다. `suggestions` 테이블이 없으면 클립보드로 복사해 톡방에 붙여넣게 한다.
  **소모임장**(= 가장 먼저 가입한 멤버)에게는 받은 목록이 같이 보이고, 눌러서 확인 표시를 토글한다
- 로그아웃

## 5. 계산 규칙 (정확히 이대로 구현, 전부 저장하지 않고 계산)

`src/lib/calc.ts` 가 스펙에 박힌 핵심 계산이고, `src/lib/stats.ts` 가 그 위에 얹는 재미·성장 지표다.
**저장하는 건 이름·날짜·거리·시간·메모·케이던스뿐이다.** 나머지는 전부 매번 계산해서, 기록을 지우면 숫자도 정확히 되돌아간다.

### 5.1 기본

- **페이스**: `total = round(duration_sec / distance_km)` → `${floor(total/60)}'${(total%60) 2자리}"` 표기. 예: 1009초/3.01km → `5'35"`
- **거리 표기**: 항상 **소수 둘째 자리 고정**. 3.01 → `3.01`, 6.2 → `6.20`. 누적이 1000km를 넘으면 정수 (390px에서 자리가 모자람)
- **주 경계**: 월요일 00:00 시작(로컬 시간 = 멤버 전원 한국, `run_date` 기준)
- **주간 스트릭**: 이번 주에 인증 ≥1건이면 이번 주 포함해 뒤로 연속인 주 수. 이번 주 0건이면 지난주부터 뒤로 센 값 + '이번 주 아직' 플래그
- **종주 진행**: `누적 = 전체 runs distance 합`. 현재 구간 = 마지막으로 `누적 ≥ 누적km` 를 만족한 마일스톤 → 다음 마일스톤.
  진행률 = `(누적-이전)/(다음-이전)`. 누적이 정확히 마일스톤 값이면 달성으로 처리
- **랭킹 집계**: 기간 내 runs를 member별로 count·sum·평균 페이스·평균 케이던스. 정렬·최소 횟수는 4.5

### 5.2 재미·성장 지표

- **에딩턴 수**: `E = max{ n : (하루 합계 거리 ≥ n km 인 날) 이 n일 이상 }`.
  같은 날 두 번 달리면 합쳐서 하루로 센다. 다음 숫자까지 남은 횟수 = `(E+1) - (E+1km 이상 달린 날 수)`.
  한 번 멀리 달려서는 안 오르고 자주 달려야 올라서, 거리와 꾸준함을 동시에 요구한다. 러닝앱들이 안 쓰는 지표라 골랐다
- **꾸준함 점수**: 최근 8주 주간 인증 횟수의 변동계수로 `100 × (1 − min(σ/μ, 1))`.
  **평균이 주 1회 미만이면 점수를 매기지 않는다** — 안 그러면 아예 안 달린 사람이 편차 0으로 만점을 받는다
- **등급**: 누적 거리로 8단계. 0 🌱새싹 러너 / 10 👟동네 한 바퀴 / 30 🏃페이스메이커 / 70 💨바람돌이 /
  150 🔥불꽃 심장 / 300 ⚡번개 다리 / 600 🦅철인 / 1000 👑전설. 경계값은 달성으로 친다
- **뱃지 12개**: 전부 계산값. 첫걸음(1회) · 열 번(10회) · 쉰 번(50회) · 두 자리(단일 10km) · 하프(단일 21.1km) ·
  5분 벽(페이스 5'00" 이내) · 누적 100 · 누적 500 · 주 3회 · 한 달 50 · 4주 연속 · 12주 연속
- **이번 달 변화**: 이번 달 1일~오늘 vs **지난달 1일~같은 날짜**. 거리 %, 인증 회, 평균 페이스 초(음수면 빨라진 것)
- **5km 예상 기록**: Riegel `T₂ = T₁ × (D₂/D₁)^1.06`. 3km 이상 기록만 재료로 쓰고, 그중 가장 좋은 값을 택한다.
  레이스를 전제한 공식이라 **순위에는 쓰지 않고 개인 추이로만** 본다
- **같이 달린 날**: 최근 n일 중 2명 이상이 같은 날 달린 날의 수. 7명짜리 소모임에서만 의미가 있는 숫자다
- **잔디 진하기**: 하루 합계 0 / <3 / <6 / <10 / ≥10km 로 5단계 고정. 분위수가 아니라 고정 임계값이어야 사람끼리 비교된다
- **피드 한마디**: 그 사람 기록 3건 이상일 때만. 개인 최고 페이스 → 최장 거리 → 평소 페이스보다 8초 이상 빠름 →
  평소 거리의 1.4배 이상 순으로 하나만. **느렸다는 말은 만들지 않는다**

## 6. 가상 종주 데이터 (`src/lib/constants.ts`에 이 그대로)

| 누적 km | 도착지 | 보상 | 이모지 |
|---|---|---|---|
| 0 | 인하대 출발 | — | 🏫 |
| 10 | 송도 | 단체 프사 갱신 | 🌉 |
| 50 | 서울 한강 | 첫 도장 기념 러닝 | 🌊 |
| 130 | 춘천 | 닭갈비 회식 | 🐔 |
| 290 | 강릉 | 물회·바다 커피 | ☕ |
| 610 | 부산 | 돼지국밥·밀면 | 🌃 |
| 900 | 여수 | 밤바다 회식 | 🌙 |
| 1200 | 전주 | 비빔밥 | 🍚 |
| 1500 | 인천 복귀 — 전국일주 완주 | 시즌 피날레 파티 | 🏁 |
| 1700 | 후쿠오카 (부산→직선 200km) | 인하대 앞 일식집 | 🍣 |

- 각 마일스톤에 SVG 좌표 `{ x, y }`를 추가한다 (`korea.svg` 뷰박스 기준)
- 도시 사이 도로 경로는 `ROUTE_SEGMENTS: { from, to, points: [x,y][] }` 배열로 `constants.ts`에 박는다. 좌표는 미리 뽑아 상수로 넣고 런타임에 계산하지 않는다

거리는 도로 기준 대략값. 페이스 산수: 1인 월 10~15km × 7명 ≈ **월 70~100km** → 송도 첫 주, 서울 3주 차, 이후 한 달에 도장 1~2개. 루트·보상은 규혁이 나중에 멤버 투표로 조정 가능(상수만 바꾸면 됨).

## 7. 디자인 시스템 (이 토큰 밖의 색·값 사용 금지)

```css
:root {
  --bg:#0B0F1A; --surface:#141B2E; --surface2:#1D2740; --line:#26314F;
  --text:#F4F6FB; --sub:#93A0BC;
  --accent:#FF6B2C; --good:#3DDC97; --bad:#FF5D73; --gold:#FFC24B;
  --grad-run: linear-gradient(90deg,#FF6B2C,#FFB020); /* 종주 진행바·강조 전용 */
  --r-card:20px; --r-btn:12px;

  /* 아래는 전부 위 토큰에서 파생한다 — 새 색을 만들지 않는다 */
  --r-chip:999px;
  --ease: cubic-bezier(.22,.61,.36,1);
  --tint: color-mix(in srgb, var(--accent) 12%, transparent);
  --heat-0: color-mix(in srgb, var(--line) 55%, var(--bg));
  --heat-1: color-mix(in srgb, var(--accent) 28%, var(--surface2));
  --heat-2: color-mix(in srgb, var(--accent) 50%, var(--surface2));
  --heat-3: color-mix(in srgb, var(--accent) 72%, var(--surface2));
  --heat-4: var(--accent);
}
```

**색이 새로 필요하면 `color-mix`로 토큰에서 파생시킨다.** 잔디 5단계도, 흐린 막대도 전부 그렇게 만들었다.
`scripts/guard.mjs` 가 `:root` 밖의 hex 를 전부 잡는다.

**색의 역할은 하나씩이다.** 이걸 어기면 화면이 무지개가 된다.

| 색 | 쓰는 자리 | 쓰면 안 되는 자리 |
|---|---|---|
| `--accent` 주황 | 나 · 지금 · 주요 동작. 잔디의 단계 색 | 한 카드에 진한 주황 두 개 |
| `--good` 초록 | **내 과거보다** 나아진 것 (페이스가 빨라짐, 스트릭이 살아 있음) | '값이 크다'는 뜻 |
| `--bad` 빨강 | 지우기 · 네트워크 실패 | **남보다 느리다** |
| `--gold` 금색 | 성취 · 1위 · 개인 최고 | 화면에 두 번 |
| `--sub` 회색 | 비교 대상 계열 전부 | 배경 위에서 opacity 를 먹이는 것 (대비가 깨진다) |

- 폰트: **Pretendard Variable** (jsdelivr CDN), fallback `-apple-system, sans-serif`. 큰 숫자는 weight 800 + `font-variant-numeric: tabular-nums`
- 무드: 야간 러닝. 다크 단일 테마. 카드 기반, 여백 넉넉히(16/20/24px), **그림자 대신** `--line` 1px 테두리 +
  카드 윗변에 빛이 걸린 듯한 1px 그라디언트 선 (`.card::before`)
- 화면당 강조 카드(`.card-hero`)는 **하나만**. 전부 강조하면 아무것도 강조되지 않는다
- 하단 탭 높이 64px + `env(safe-area-inset-bottom)`. 업로드 탭은 `--grad-run` 원형 버튼
- 이모지는 포인트로만(화면당 2~3개). 기능 자리는 `src/components/Icon.tsx` 의 선 아이콘을 쓴다 —
  24×24 격자에 직접 그린 path라 라이선스가 없다
- 차트는 **라이브러리 없이** CSS 격자와 인라인 SVG로만 (`src/components/charts.tsx`):
  막대 · 잔디 · 추이 선 · 원형 진행률 · 비교 막대. **값이 0인 칸에 색 조각을 남기지 않는다** — 달린 것처럼 보인다
- 모션: 등장 stagger(6개까지) · 누를 때 `scale(.97)` · 카운트업 · 펜 그리기 · 현재 위치 점 숨쉬기 ·
  뱃지/도착 pop. `prefers-reduced-motion` 이면 전부 끄고 최종 상태만 (로딩 스피너는 상태 표시라 남긴다)
- 뷰포트 기준 390px. 610px 이상은 중앙 max-width 480px 컨테이너
- **Tailwind·CSS 프레임워크 금지**

## 8. 기술 스택 (확정 — 변경 금지)

| 구분 | 확정 | 비고 |
|---|---|---|
| 프론트 | Vite + React + **TypeScript** | 타입 에러 = 공짜 검증 |
| 라우팅 | react-router-dom **HashRouter** | GitHub Pages에서 새로고침 404 방지 |
| DB | Supabase (무료) | 서버 코드 없음. 전부 클라이언트에서 supabase-js. Storage 안 씀 |
| 상태 | `src/lib/store.ts` (모듈 변수 + `useSyncExternalStore`, 40줄) | 소모임 데이터 한 벌을 한 번만 받아 모든 화면이 나눠 쓴다. **상태관리 라이브러리 아님** |
| 배포 | **GitHub Pages** (Actions) | main push마다 |
| 테스트 | vitest — **계산·파싱 함수만** | UI 테스트 금지 (Simplicity) |
| 화면 검증 | Playwright + 가짜 REST (`scripts/`) | 운영 DB를 안 건드리고 전 화면을 찍는다 |

**의존성 화이트리스트 (이 외 설치 금지):** `react` `react-dom` `react-router-dom` `@supabase/supabase-js` `browser-image-compression` `date-fns` `tesseract.js` / dev: `typescript` `vite` `@vitejs/plugin-react` `vitest` `playwright` `@types/react` `@types/react-dom`

밤샘 개편에서 새로 넣은 것(차트·아이콘·공유 카드·PWA·잔디)은 **의존성을 하나도 늘리지 않았다.**
차트는 CSS+SVG, 아이콘은 직접 그린 path, 공유 카드는 canvas 2D, 아이콘 PNG는 `scripts/icons.mjs` 가 한 번 구워 커밋한다.

## 9. 데이터베이스 (`supabase/migrations/`)

```sql
create table members (
  id uuid primary key default gen_random_uuid(),
  name text unique not null,          -- 로그인 아이디 겸 표시 이름 (trim 후 저장)
  pw_hash text not null,              -- 숫자 4자리의 해시
  emoji text not null default '🏃',
  created_at timestamptz not null default now()
);

create table runs (
  id uuid primary key default gen_random_uuid(),
  member_id uuid not null references members(id) on delete cascade,
  run_date date not null,
  distance_km numeric(5,2) not null check (distance_km between 0.1 and 60),
  duration_sec integer not null check (duration_sec between 60 and 21600),
  memo text check (char_length(memo) <= 60),
  created_at timestamptz not null default now()
);

alter table members enable row level security;
alter table runs enable row level security;
create policy members_select on members for select using (true);
create policy members_insert on members for insert with check (true);
create policy runs_select  on runs for select using (true);
create policy runs_insert  on runs for insert with check (true);
create policy runs_delete  on runs for delete using (true);
```

**Storage를 쓰지 않는다.** 사진은 브라우저에서 OCR로 읽고 버리므로 버킷도 정책도 없다. 0001을 이미 적용한 프로젝트는 `0002_drop_screenshot.sql`로 정리한다:

```sql
alter table runs drop column screenshot_url;

drop policy if exists screenshots_select on storage.objects;
drop policy if exists screenshots_insert on storage.objects;
drop policy if exists screenshots_delete on storage.objects;

-- 버킷은 비어 있어야 지워진다 (storage.objects → storage.buckets 외래키)
delete from storage.objects where bucket_id = 'screenshots';
delete from storage.buckets where id = 'screenshots';
```

- **비밀번호**: 숫자 4자리. `pw_hash = SHA-256(name + ':' + pw)` hex, WebCrypto로 클라이언트에서 계산 (평문 저장 금지 — 해시는 공짜다)
- **보안 수준 명시 (그대로 받아들일 것):** 4자리는 1만 가지뿐이고 시도 횟수 제한도 없다. anon key도 공개 전제라 코드에 커밋한다. 즉 링크를 아는 사람은 이론상 남의 이름으로 들어가거나 데이터를 조작할 수 있다 — 7명 신뢰 기반 동아리 도구이고, 웹에 남는 건 **이름·날짜·거리·시간·메모뿐**이라(사진은 브라우저에서 읽고 버려져 서버에 올라가지 않는다) **의도된 트레이드오프**다. 링크는 톡방에만 공유. 여기에 서버 인증·이메일 인증·비번 복잡도 규칙을 붙이는 과잉 설계 금지 (2차에 필요해지면 Edge Function으로 올린다)
`0003_fun.sql` 이 케이던스 칸·건의함·응원을 **한꺼번에** 넣는다. 셋을 따로 적용하지 않는 게 중요하다 —
앱은 `suggestions` 테이블 하나만 물어보고 셋 다 있다고 판단한다 (`src/lib/store.ts` 의 `probeCaps`).
없는 테이블을 세 번 물으면 콘솔에 404가 세 번 찍히고, 그건 '콘솔 에러 0' 규칙과 싸운다.

```sql
alter table runs add column if not exists cadence_spm smallint;   -- 100~250 체크 제약 포함

create table suggestions (id, member_name, text(1~300), done bool, created_at);
create table cheers      (id, run_id → runs, member_id → members, created_at, unique(run_id, member_id));
-- RLS 수준은 기존과 같다. suggestions 는 확인 표시 토글을 위해 update 정책까지 연다
create index cheers_run_idx on cheers (run_id);
create index runs_member_date_idx on runs (member_id, run_date desc);
```

**`runs` 에 update 정책은 열지 않는다.** 기록 고치기는 '새로 넣고 옛 걸 지운다'로 구현했고
(그 편이 네트워크가 끊겼을 때 안전하다), 정책을 하나 덜 여는 게 낫다.

**Claude 에게 이 프로젝트 권한이 없다.** 스키마를 바꿔야 하면 마이그레이션 SQL을 쓰고,
**규혁이 콘솔에서 돌리기 전까지 앱이 그 기능 없이도 멀쩡히 돌아가게** 만든 뒤, 붙여넣을 SQL을 대화로 알려준다.

- 시드: 개발용 데모 시드(`seed_demo.sql`, 멤버 3~4 + 기록 12건)로 화면을 채워 개발하고 **배포 인수 전 데모 멤버·기록을 전부 삭제**해 빈 상태로 넘긴다. 실제 멤버는 각자 첫 로그인 때 스스로 생긴다 — 명단을 미리 넣지 않는다

## 10. 파일 구조 (이 구조 밖 파일 생성 금지)

```
deep-running/
├─ SPEC.md  CLAUDE.md  README.md  .gitignore   ← 문서는 이 셋이 전부
├─ index.html  package.json  vite.config.ts  tsconfig.json
├─ .github/workflows/  ci.yml  deploy.yml
├─ .claude/  settings.json  agents/{design-reviewer,copy-auditor,spec-auditor}.md
├─ docs/     home.png  feed.png  my.png        ← README 스크린샷 (배포엔 안 실린다)
├─ public/   og.jpg  manifest.webmanifest  icon-192.png  icon-512.png  apple-touch-icon.png
├─ scripts/  fixtures.mjs  rest-mock.mjs  shots.mjs  guard.mjs  icons.mjs   ← 개발 도구
├─ supabase/
│  ├─ migrations/  0001_init.sql  0002_drop_screenshot.sql  0003_fun.sql
│  └─ seed_demo.sql
├─ src/
│  ├─ main.tsx  App.tsx  styles.css
│  ├─ assets/     korea.svg
│  ├─ lib/        supabase.ts  store.ts  auth.ts  calc.ts  stats.ts  parse.ts  ocr.ts
│  │              constants.ts  card.ts  ko.ts  anim.ts
│  ├─ components/ Layout.tsx  TabBar.tsx  Icon.tsx  charts.tsx  Guard.tsx
│  │              RunCard.tsx  RunEditor.tsx  Journey.tsx  Roster.tsx  Badges.tsx
│  │              Suggest.tsx  InstallHint.tsx  Boom.tsx  EmptyState.tsx
│  └─ pages/      Login.tsx  Home.tsx  Feed.tsx  Upload.tsx  Ranking.tsx  My.tsx
└─ tests/  calc.test.ts  parse.test.ts  ocr.test.ts  stats.test.ts
```

`scripts/guard.mjs` 가 이 구조를 검사한다. **구조를 넓혀야 하면 이 목록을 먼저 고친다.**

- `scripts/` 는 개발 도구다. vite 빌드 밖이라 배포 번들에 들어가지 않는다
  - `fixtures.mjs` 가짜 데이터(가득 찬 상태 / 실제와 같은 1명·1건 상태) · `rest-mock.mjs` PostgREST 흉내 ·
    `shots.mjs` 전 화면 스크린샷 + 한 장짜리 대지 · `guard.mjs` 규율 검사 · `icons.mjs` 홈 화면 아이콘 굽기
- README 스크린샷은 `docs/`, 크롤러가 절대 URL로 가져가야 하는 og·PWA 아이콘은 `public/`

## 11. 작업 규칙 — CLAUDE.md

작업 규칙은 저장소 루트의 **CLAUDE.md** 하나에만 둔다. 여기에 같은 내용을 또 적으면
둘이 어긋나기 시작하고, 그때 어느 쪽이 맞는지 아무도 모르게 된다.

요약하면: Karpathy식 4원칙 · 한국어 해요체 · 토큰 밖 색 금지 · 계산값 우선 ·
`npm run verify` 한 줄로 검증 · `npm run shots` 로 화면을 직접 보기 ·
`.claude/agents/` 의 검토 에이전트에게 남의 눈을 빌리기 · main 직접 작업, 큰 기능만 브랜치.

## 12. 실행 계획 — Phase 게이트 (순서 고정, 판정 전부 통과 후 다음으로)

Phase 0~7은 main에 완료됐다. 이후로는 **main에서 바로 작업하고 자주 커밋한다.** 큰 기능일 때만 `feat/*` 브랜치를 판다. CI는 main push와 PR 양쪽에서 돌아 빌드가 깨진 채로 배포되는 것만 막는다.

### Phase 0 — 셋업·배포 파이프라인
Vite+React+TS 스캐폴드, styles.css에 7장 토큰, HashRouter + 탭 5개 빈 페이지, GitHub 저장소 생성(gh CLI), Pages용 Actions 워크플로(vite `base:'./'`), Pages 활성화(gh api).
**판정:** build 에러 0 · 배포 URL이 200으로 열리고 탭 이동이 됨 · 390×844 스크린샷 확인

### Phase 1 — DB
0001_init.sql 작성·적용(Supabase MCP 또는 SQL Editor), screenshots 버킷 생성, seed_demo.sql 적용, supabase.ts 클라이언트.
**판정:** 앱에서 members select가 시드 멤버를 반환 · 시드 runs가 조회됨 · 새 members insert가 RLS에 막히지 않음

### Phase 2 — 로그인
4.1 전체.
**판정 시나리오 4개(각각 스크린샷):** ① 새 이름 입력 → 확인 다이얼로그 → 계정 생성·로그인 ② 로그아웃 후 같은 이름+비번으로 재로그인했을 때 이전 기록이 그대로 보임 ③ 기존 이름 + 틀린 비번 → 에러이고 계정이 중복 생성되지 않음 ④ 이름 앞뒤에 공백을 넣어도 같은 계정으로 들어감. 새로고침해도 세션 유지

### Phase 3 — 업로드
4.4 전체 + `parse.ts`(시간 파싱).
**판정:** tests/parse.test.ts 통과 — `"16:49"→1009` `"1:05:55"→3955` `"28"→1680` `"0"→거부` `"abc"→거부` · 실제 이미지 1장 업로드→runs insert→피드에 보임 (Storage 는 Phase 7에서 없앴다)

### Phase 4 — 피드·마이
4.3, 4.6 + `calc.ts`(페이스·스트릭·집계).
**판정:** tests/calc.test.ts 통과 — 페이스 포맷 2케이스(5장 예시 포함), 스트릭 3케이스(연속/이번주없음/공백주), 주 경계(일→월 넘어가는 케이스) · 시드 데이터의 마이 통계를 손계산과 대조해 일치 · 삭제하면 피드·통계에서 즉시 반영

### Phase 5 — 홈(종주)·랭킹
4.2, 4.5 + constants.ts(6장 표 그대로).
**판정:** 누적=마일스톤 정확 도달 경계 테스트 1케이스 통과 · 시드 누적 합이 화면 숫자와 일치 · 랭킹 동률 정렬 확인 · 스크린샷 확인

### Phase 6 — 폴리시·인수
빈 상태 전 화면, 로딩 스피너, favicon·og 태그(제목 "Deep Running"), 데모 기록 삭제(멤버는 유지), README를 포트폴리오 품질로(소개 문단, 스크린샷 3장, 스택·아키텍처 4~5문장, 실행법, 결정 로그), 최종 배포.
**판정:** 전 화면 스크린샷 세트 최종 확인 · 아래 15장 성공 기준의 "출시 조건" 전부 충족 · 규혁에게 "폰 5분 점검 목록"(로그인→업로드→피드→홈 숫자→삭제) 전달

### Phase 7 — 개발 방식 전환 · 사진 비저장 · OCR  [브랜치 `feat/ocr`]
`ci.yml` 추가(pull_request에서 `tsc --noEmit` · `vitest run` · `vite build`). 거리 표기 소수 둘째 자리 고정(3.01이 3.0으로 보이던 버그). `0002_drop_screenshot.sql`. 사진 업로드·Storage 제거. RunCard 재설계. `ocr.ts` + tesseract.js 자동 채움. README·og 이미지를 저장소로 이관.
**판정:** PR에서 CI 3개 초록 · calc 테스트에 거리 표기 케이스(3.01→`3.01`, 6.2→`6.20`) · ocr 테스트 통과 · 0002 적용 후 runs에 screenshot_url 없고 screenshots 버킷 없음 · 사진 없이 인증 저장됨 · 삼성헬스·나이키런클럽 화면 각 1장에서 거리·시간이 폼에 채워짐 · 무관한 사진에서 에러 문구 없이 조용히 넘어가고 직접 입력됨 · 피드·업로드 390×844 스크린샷 확인
→ 완료 (PR #1 머지)

### Phase 8 — 실제 지도 종주  [브랜치 `feat/journey-map`]
`korea.svg` + 좌표·경로 상수 + `stroke-dasharray` 펜 애니메이션.
**판정:** 누적 0 / 구간 중간 / 완주 3케이스 렌더 확인 · 390px에서 안 깨짐 · 런타임 네트워크 호출 0건(Playwright로 확인) · 의존성 추가 0개 · CI 초록

### Phase 9 — 밤샘 개편  [브랜치 `feat/night`]
작업 환경(`scripts/` harness · `npm run verify` · 규율 검사 · 검토 에이전트 3종) · 재미 지표 층(`stats.ts`) ·
전 화면 디자인 개편(차트·아이콘·모션) · 기록 고치기 · 건의함 · 응원 · 케이던스 · 톡방 공유 카드 · PWA.
**의존성 추가 0개.**
**판정:** `npm run verify` 초록 · `npm run shots` 17장을 눈으로 확인(가득 찬 상태 + 1명·1건 상태 둘 다) ·
검토 에이전트 3종의 🔴 항목 전부 반영 · CI 초록 · 0003 SQL을 규혁에게 전달

## 13. 사람(규혁)이 할 일

**지금 당장 (5분):**

1. **`supabase/migrations/0003_fun.sql` 을 Supabase SQL Editor에 통째로 붙여넣고 실행.**
   케이던스 입력·랭킹, 건의함(소모임장 받은 목록 포함), 응원 하트가 그 순간 켜진다.
   안 돌려도 앱은 멀쩡히 돌아간다 — 그 셋만 안 보인다. 여러 번 돌려도 안전하다
2. 폰으로 5분 점검: 로그인 → 업로드 → 보상 화면 → 피드 → 홈 숫자 → 마이 → 기록 고치기 → 지우기
3. 폰 브라우저 공유 → **홈 화면에 추가**. 아이콘으로 들어가야 사람들이 계속 쓴다
4. 톡방에 링크 고정 + 안내 한 줄: "이름이랑 숫자 4자리 정해서 치면 끝, 다음부터 같은 걸로 들어오면 돼요"

**계속:**

5. 스키마 변경 SQL은 Supabase SQL Editor에서 직접 실행 (Claude에 프로젝트 권한이 없다 — SQL은 Claude가 그대로 출력해준다)
6. 큰 기능은 PR로 올라오니 리뷰하고 머지 (작은 변경은 main에 바로 들어간다)
7. 루트·보상·뱃지·등급 이름이 마음에 안 들면 `src/lib/constants.ts` 와 `src/lib/stats.ts` 의 상수만 고치면 된다.
   전부 계산값이라 이미 쌓인 기록에 바로 반영된다

멤버 명단은 준비할 필요 없다. 각자 첫 로그인 때 계정이 생기고, 신입이 들어와도 링크만 주면 된다.

## 14. 폰에서 지시·확인하는 법

개발 지시·진행 확인은 **claude.ai의 Claude Code 웹(클라우드 세션)** 으로 폰 브라우저·앱에서 가능하고, PC에서 돌리던 로컬 세션을 폰에서 이어받는 **Remote Control**도 있다. 결과물 자체가 모바일 웹이라 배포 URL만 열면 폰에서 바로 확인된다. 즉 "폰으로 시키고 → 폰으로 확인"이 된다.

## 15. 리스크 · 성공 기준

| 리스크 | 대응 |
|---|---|
| 이중 업로드 귀찮음 (최대 리스크) | 사진 한 장 고르면 거리·시간이 자동으로 채워져 입력이 몇 초로 줄어든다. 2차 '인증 카드'로 웹 업로드→톡방 공유 순서 역전 |
| 초반 참여 절벽 | 첫 2주 소모임장 리마인드 + 과거 톡방 인증 소급 입력으로 첫 화면 채우기 |
| 수동 입력 뻥튀기 | **사진 인증은 톡방이 담당한다.** 웹은 숫자만 받고 7명 상호 검증에 기댄다 (신뢰 기반) |
| 링크 유출 | 톡방에만 공유. 문제 생기면 2차에서 초대코드/Edge Function |

**출시 조건(Phase 6 판정):** 폰에서 로그인→업로드→피드 노출까지 30초 내 · 콘솔 에러 0 · 전 화면 한국어·빈 상태 존재 · README 완성
**성공 기준(운영):** 개강 2주 내 7명 중 5명 이상 업로드 ≥1회 · 4주 차 주간 인증 5건+

## 16. 이 스펙에 반영한 기법 (왜 이렇게 생겼나)

**채택:** ① Karpathy식 4원칙을 범용 문구가 아니라 이 프로젝트 전용 규칙으로 CLAUDE.md에 내장 ②
완료 판정을 먼저 정하고 통과해야 진행 ③ **화면을 직접 보는 장치** — Playwright + 가짜 REST로 전 화면을 찍고
한 장짜리 대지로 모아본다. 운영 DB를 안 건드리고, 데이터가 가득 찬 상태와 **실제와 같은 1명·1건 상태**를 둘 다 본다
④ 계산·파싱 함수만 유닛테스트 ⑤ **기계가 볼 수 있는 규율은 기계에게 맡긴다**(`scripts/guard.mjs` — 파일 구조·
토큰 밖 색·영어 문구·의존성 화이트리스트를 CI에서 검사) ⑥ **검토 에이전트에게 남의 눈을 빌린다**
(디자인·한국어 문구·스펙 정합 셋. 혼자 오래 보면 눈이 먼다 — 실제로 이 방식이 '0인 주에 남는 주황 조각',
'다크 배경의 검은 달력 아이콘', '한 달을 다 못 채운 이번 달을 지난달 전체와 비교하던 것'을 잡았다)
⑦ 결정 로그(멈추지 않되 추적 가능) ⑧ 스펙 단일 진실 — 같은 내용을 두 문서에 적지 않는다
⑨ **근거 있는 제품 결정**: 리텐션·랭킹 설계는 STEP UP RCT, Duolingo 스트릭, Strava Local Legend,
GitHub 기여 그래프 같은 사례를 조사해서 정했고, 근거가 약한 것(ACWR 부상 위험, VDOT)은 일부러 뺐다

**제외:** 그래프 오케스트레이션(이 규모엔 과함), 범용 스킬 설치기, MCP 빌더, 서비스 워커
(오프라인 이득보다 낡은 앱이 캐시에 남는 위험이 크다 — manifest만으로도 홈 화면 추가는 된다),
상태관리 라이브러리(모듈 변수 40줄로 충분했다).
